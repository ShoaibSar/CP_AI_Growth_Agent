# OpenAI API key for the local streaming proxy.
# The key stays on the server; it is never exposed to the browser.
OPENAI_API_KEY=your_openai_api_key_here

# Optional overrides
OPENAI_MODEL=gpt-5.5
PORT=8787
